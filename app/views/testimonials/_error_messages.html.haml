- if @testimonial.errors.any?
  %div{id: "error_explanation"}
    .alert.alert-error
      The form contains
      = pluralize(@testimonial.errors.count, "error") + "."
    %ul
      - @testimonial.errors.full_messages.each do |msg|
        %li
          *
          = msg